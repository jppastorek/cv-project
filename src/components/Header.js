import React from "react";

//this will be the header of the App. it will just be a bar with a title in the left corner

const Header = (props) => {
    //some code

    return (
        //some more code
        <header>
            <h2>CV Builder</h2>
        </header>
    );
}

export default Header;